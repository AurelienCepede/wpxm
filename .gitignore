# Secrets & local config
.env

# Runtime data (created by docker compose / wordpress)
db/
www/

# Plugin archives (binaires, à fournir hors-repo)
plugins/*.zip

# DB import dumps (see import-db/ — only the placeholder is tracked)
import-db/*
!import-db/put backup.sql file here

# TLS certificates (generated by mkcert, contain private keys)
traefik/certs/

# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp
*~
